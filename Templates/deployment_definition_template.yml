apiVersion: 
kind: 
metadata:
  name: 
  labels:
    app:
    type: 

spec:
  template:
        metadata:
         name: 
         labels:
            app:

        spec:
          containers:
            - name: 
              image: 
  replicas: 

  selector:
    matchLabels:
      app: